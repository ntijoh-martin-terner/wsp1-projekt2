<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todoer</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <script defer src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg shadow">
        <div class="container-fluid d-flex align-items-center justify-content-between py-2" style="height: 80%;">
            <!-- Logo and Title -->
            <a href="/fruits" class="text-decoration-none d-flex align-items-center">
                <img src="/img/affaren.png" class="logo-image" alt="Todoer Logo" style="height: 50px; width: auto; object-fit: cover;">
                <h1 class="logo mb-0 ms-3 fw-bold">Todoer</h1>
            </a>

            <!-- Navbar Toggle Button for Mobile -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navbar Links and Right-side Wrapper -->
            <div class="collapse navbar-collapse justify-content-between h-100" id="navbarNav">
                <!-- Navigation links -->
                <ul class="navbar-nav align-items-center ms-lg-5">
                    <li class="nav-item">
                        <a class="nav-link" href="/home">Home</a>
                    </li>
                </ul>

                <div class="d-flex align-items-center ms-auto gap-3 h-100">
                    <!-- Logout button -->
                    <form action="/logout" method="POST" class="ms-auto h-100">
                        <button type="submit" class="btn btn-outline-danger btn-sm align-self-stretch d-flex align-items-center h-100">
                            <i class="bi bi-box-arrow-right"></i> Logout
                        </button>
                    </form>
                </div>


            </div>
        </div>
    </nav>

    <main class="container my-4">
        <%= yield %>
    </main>

    <footer class="text-center py-3 mt-auto">
        <p class="mb-0">Contact us</p>
    </footer>
</body>

</html>